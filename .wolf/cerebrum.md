# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-08-19 — Key Learnings reorganized by topic (was purely chronological); no content removed, only regrouped under subheaders.

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->
- **No example/demo text in input placeholders.** User pushed back hard on a `placeholder="למשל: הבלם נשייף בחישוק"` (example text) in a new free-text reason field, questioning the example itself ("מה זה נשייף בכלל?"). Don't invent illustrative example content for placeholder text on new form fields in this app — leave placeholders empty (or omit the attribute) unless the user asks for one, rather than guessing at a plausible-sounding example.
- **Address the user in masculine Hebrew grammar (זכר), not feminine** — see [[feedback_user_is_male]] in cross-session memory. He corrected this directly: "חלאס לקרוא לי בנוסח נשי אני גבר!"

## Key Learnings

- **Project:** wheels-app

### Vehicle Data, Fitment & PCD Matching

- **vehicle_models table**: עמודת `make_he` היא NOT NULL. בעת הוספת רכב ב-API route, אם `make_he` לא נשלח, חייבים לתרגם אוטומטית מ-English → Hebrew.
- **translateMakeToHebrew**: הפונקציה ב-`/api/vehicle-models/route.ts` מחפשת קודם ב-`englishToHebrewMakes` (היפוך של `hebrewToEnglishMakes`), אחר כך Google Translate API עם `GOOGLE_VISION_API_KEY`, ואז פולבק לשם האנגלי.
- **handleAddScrapedResult** בדף admin/vehicles: שולח `make_he: plateVehicleInfo?.manufacturer_he || ''` — כשב-manual mode `plateVehicleInfo` הוא null ולכן `make_he` ריק. הפתרון הוא בצד שרת, לא ב-client.
- `wheels.tire_size` (free text, e.g. "205/55") and the vehicle lookup's `vehicle.front_tire` (full "WWW/AARdd" incl. rim) are NOT the same format — `wheels.tire_size` deliberately omits the rim suffix since rim size is captured separately in `wheels.rim_size`. `getTireDiameterMm()` in src/lib/vehicle-mappings.ts accounts for this via an optional rim-size override parameter. Reused by both src/app/search/page.tsx and src/app/stations/page.tsx, which duplicate the same per-wheel result-card rendering logic (rimMismatch/cbNeedsRing/diameterMismatch) — keep both in sync when touching one.
- [2026-07-03, RESOLVED same session] **ET/offset is NOT the real cause of caliper-rubbing fitment problems** — user corrected this directly. Resolution shipped: removed the `*` required-marker and the ET filter/display (card badge + table column) from `src/app/[stationId]/page.tsx` — the `offset` input field itself stays (optional, still stored, no schema/API change). `public/guide.html` section 6 rewritten to state ET is not a reliable check and to instruct managers to record known-problematic wheels/models in the wheel's existing `notes` field instead. Logged as bug-092 in buglog.json. `public/report.html` (the analysis doc) was NOT updated in this pass — still says "ET: decide whether to add input or remove filter" as an open recommendation; sync it if asked. The guide PDF (`public/ניתוח-מערכת-גלגלים-ידידים-v3.pdf`, part ג׳ §3) frames "wheel rubs on brake" as an ET/offset problem, and the July 2 commit (e962846) added a numeric `offset` field to wheels accordingly (marked `*`/required-looking in the form, same as PCD/rim, but NOT actually enforced in validation — see [stationId]/page.tsx:1406-1407 vs 1428). User says this is a fundamental misunderstanding: the real blocker is the internal barrel/hoop shape ("צורת החישוק הפנימי") of specific wheel models and its diameter near the hub — not the ET number. This is consistent with the guide's own documented field incidents (Kia Rio spare, BYD Atto 3) which were solved empirically (front↔back swap, since rear calipers are usually smaller) rather than by computing ET, with an explicit caveat that "seemingly identical" calipers can still differ (Lexus exception) — i.e. it's tacit/model-specific knowledge, not a formula. **Implication: no single numeric field (ET, CB, PCD) can predict this risk; it may need per-model/per-wheel known-issue tagging instead of numeric matching.** Don't build ET-based automatic compatibility/mismatch logic as if it solves the caliper-rubbing risk — it doesn't, per the user.
- [2026-07-03] **The uploaded HTML guides (`public/guide.html`, `public/report.html`) chronically lag behind what the code actually does — verify every "gap"/"missing" claim in them against the code before trusting it.** Confirmed twice in one session: (1) the universal-PCD feature was already self-corrected once before this session ("גרסה קודמת... טעתה כשקבעה שהתכונה חסרה"), and (2) the tire-diameter mismatch warning was documented as "data saved, no calc/warning" when `getTireDiameterMm()` (src/lib/vehicle-mappings.ts) + a 3%-threshold warning were already fully wired into both search/page.tsx and stations/page.tsx result cards, from the same 2026-07-02 commit (e962846) that the docs otherwise describe accurately for ET. Don't assume a "missing feature" claim in these docs is current — grep the actual code first.
- [2026-07-05, RESOLVED same session] **`vehicle_models.rim_sizes_allowed` ("allowed rim sizes") was a fundamentally unreliable field, now removed from all matching/display** — real screenshot evidence (Mazda 6, plate 7000968, own tire 205/60R16 → own rim 16"): the DB said allowed sizes were "17-20" — a range that didn't even include the car's own 16" — so donor wheels of the car's *exact own size* (16") were flagged red "קוטר לא תואם", while 18" wheels (2 sizes off) passed clean, because `effectiveRimSizes` (search/page.tsx, stations/page.tsx) fully overrides the vehicle's own rim whenever `rim_sizes_allowed` is non-empty, regardless of whether it's a sane value. Root cause of the bad data: `rim_sizes_allowed` is `text[]` in the live DB despite the TS type claiming `number[]` (bug-001, old), an admin free-text edit box silently truncates a typed dash-range like "17-20" via `parseInt` (`admin/vehicles/page.tsx`, was ~line 1058), and `api/vehicle/lookup/route.ts` silently re-scrapes wheelfitment.eu and overwrites the field on every plate lookup lacking `source_url` — no logging either way. **Fix (bug-109): replaced the whole concept everywhere** with `checkRimFit()`/`checkVehicleRimFit()` in `src/lib/vehicle-mappings.ts` — donor rim must be within the vehicle's own rim ±1" (from its own tire spec via `extractRimSize`), validated by the existing `getTireDiameterMm()` 3%-relative-diameter check (which already accepted a `rimSizeOverride` param — built for exactly this). Added a third status `needs_tire_data` (rim in-window, no tire_size on record to confirm diameter) instead of the old silent "no data = pass" pattern. Donut/spare wheels still go through the ±1 rim window but stay exempt from the diameter check itself (unchanged carve-out). Touched: search/stations pages (core matching+display), operator/lookup pages (display only), reverse-search page+API (adapted to vehicle-vs-vehicle comparison, no wheel inventory involved), admin/vehicles/page.tsx (removed the filter/column/edit-form UI — the edit form was one of the two corruption sources), and the two silent auto-write paths (`api/vehicle/lookup/route.ts`, `api/admin/scrape-wheelfitment/route.ts` batch job) which no longer touch the field. **DB column itself was NOT dropped** (no DB access this session) — deliberately deferred to a future manual migration once the new logic is verified live. `admin/vehicles/page.tsx`'s scrape-preview-to-add-vehicle flow and `ScrapeResult` type were deliberately left untouched (out of scope, harmless — nothing reads the field back for matching anymore either way). **Regression caught by regression-dog before commit and fixed same session:** the first version of `checkRimFit()` dropped `manualRimSize` (the UI's manual rim picker for vehicles with no tire data at all, e.g. personal imports — see `stations/page.tsx:62` comment) from matching entirely, since `checkRimFit` only called `extractRimSize()` on the vehicle's own tire string. Fixed by adding an optional `vehicleRimFallback` param to `checkRimFit()` (mirrors `checkVehicleRimFit`'s existing `candidateRimFallback`), wired as `checkRimFit(front_tire, wheelRim, wheel.tire_size, wheel.is_donut, manualRimSize)` in both search/stations pages — the manual pick now anchors the ±1 window even though no tire profile exists to validate diameter against (diameter check gracefully no-ops to 'match' in that case, same permissive-on-missing-data pattern used elsewhere).
- [2026-07-07, RESOLVED same session] **`findPcdData`/`searchDb()` in `src/app/api/vehicle/lookup/route.ts` used ILIKE substring matching in the WRONG direction, causing generic model queries to match more specific trim rows with different PCD.** User reported plate 5843659 (real 2014 Subaru Impreza, confirmed via live data.gov.il query: `kinuy_mishari="IMPREZA"`, `shnat_yitzur=2014`) returned PCD 114.3 instead of the correct 100. Root cause: `.ilike('model', '%${modelLower}%')` requires the DB's `model` column to CONTAIN the query string — so query "impreza" matched DB row "impreza wrx sti" (a distinct performance trim, bolt_spacing 114.3) because that substring relationship holds, and no DB row for plain "impreza" existed covering exactly year 2014 (only a +2017 row), so the WRX STI row's 2007-2014 range was the only year-filter match. Fixed by flipping the containment direction: fetch all make+year candidates and require `modelLower.includes(dbModel)` (query contains DB text, not vice versa) in JS, scoring matches by specificity (prefer the longest/most-specific DB model or variant text still contained in the query). This also subsumed the old "no-spaces" and "first-word" fallback tiers since full-query containment already covers those cases. When nothing matches under the new rule, `searchDb()` correctly returns null and `findPcdData()` falls through to the live wheelfitment.eu scrape rather than guessing — consistent with this codebase's existing "external verified source over guessed DB row" philosophy (see the ET/rim_sizes_allowed learnings above). Logged as bug-120.
- [2026-07-26, RESOLVED same session] **`search/page.tsx`'s OCR→auto-search flow (`handleOcrSearch`) only reset `ocrAutoSearch` to `false` in one of its three branches** (the "manufacturer/model only, no year, no plate" branch) — the other two (model search, plate-lookup fallback) left it `true` forever after completing, even on error. Since `ocrAutoSearch=true` hides the entire manual search form/tabs (`{!ocrAutoSearch && (<>...` in the vehicle modal JSX) and the plate-tab error box lives outside that block but is easy to miss with no surrounding UI, a failed OCR→plate lookup reads to the user as "the search just cancelled for no reason." Also: `canModelSearch` requires `manufacturer && model && year` ALL present — OCR (Gemini) reads year from small print on the license and fails more often than manufacturer/model, so a successful make/model read still silently falls back to plate search whenever year didn't come through. Fixed the stuck-UI bug (added `setOcrAutoSearch(false)` to the other two branches). Deliberately did NOT relax the `year` requirement — loosening it risks wrong PCD matches across model years (see bug-120's lesson about model-matching precision) and needs a product decision, not just a code fix. Logged as bug-123.
- [2026-08-09, RESOLVED same session] **`findPcdData`/`searchDb()` in `src/app/api/vehicle/lookup/route.ts` could silently return an unverified `vehicle_models` row instead of an available verified one, because it had no tie-break when two candidate rows matched with equal text specificity.** Reported by the user as "אמת מידות" link disappearing for Honda Jazz after a search. Root cause had two independent facets: (1) a standalone "Step 1" query matched `technicalModelLower` (gov `degem_nm`) against `variants` via `.ilike(...).limit(1)` and returned **immediately** on any hit, never considering `source_url`/specificity against the rest of the table; (2) even without that, `vehicle_models` has ~**354 rows** with `source='import'`, `source_url IS NULL`, an open-ended `year_to: null`, and `variants` holding a bare Hebrew commercial name (or, for Jazz, even the same English `model` text, e.g. "jazz") — these legacy bulk-imported rows can tie an equally-specific, properly year-scoped, wheelfitment.eu-verified row, and win purely on arbitrary Supabase row order. **Fix:** removed the separate Step 1 early-return and folded technical-variant matching into the same unified candidate-scoring loop as the commercial model/variant match, then added an explicit tie-break: prefer a row with a non-null `source_url` (verified) over one without, then prefer a row with a real (non-null) `year_to` over an open-ended one. Verified live against data.gov.il plates for Honda Jazz (2012, 2018 — both now resolve to the correct year-scoped wheelfitment.eu URL) and Toyota Camry (2001 — unaffected/still correct). **Deliberately NOT touched:** the 354 legacy rows themselves (no DB write this session) and the pre-existing, unrelated behavior where a genuinely *more specific* but still-unverified trim row (e.g. "camry hybrid", import-sourced) legitimately outranks a less-specific verified row ("camry", wheelfitment.eu) — that's working as designed per the 646f345 Impreza/WRX-STI fix and is a separate, real gap (that trim's data has just never been successfully scraped) rather than a tie-break bug. Logged as bug-144.
- [2026-08-19, RESOLVED same session, root cause corrected mid-session] **`src/app/api/vehicle/lookup/route.ts` returned 404 for plate 5138775, a real, currently-registered vehicle the user drives daily — TWO independent bugs stacked, and my first diagnosis was wrong.** First pass (logged as bug-148): I "verified" both data.gov.il resources had 0 records for this plate by querying them the same way the app does — `q=<plate>` (full-text search) — and concluded the gov databases genuinely lacked the vehicle, with find-car.co.il as the only real gap (its HTML now renders each label/value on its own indented line, e.g. `<span>\n  שם תוצר\n</span>\n<strong>\n  פורד\n</strong>`, which broke the scraper's tight `label</span>` regexes for every plate, not just this one — that fix, adding `\s*` around labels/values in `scrapeFromFindCar()`, was real and still correct). **User pushed back ("it's my car, I drive it every day") and that pushback was right**: `filters={"mispar_rechev":5138775}` (exact field match) against the SAME regular-vehicles resource (`053cea08-...`) returns the full real record (Ford Focus/פורד גרמניה, DYB TREND, 2012, baalut: פרטי) — while `q=5138775` on the identical resource returns 0. **data.gov.il/CKAN's `q` full-text search index can be stale/out of sync with the underlying table for individual rows, even when the row unquestionably exists and is queryable by exact filters.** The app used `q=` for both gov.il lookups, so ANY plate whose row happened to be missing from that index — not just genuinely-absent ones — would false-negative into a 404. Fixed by switching both `datastore_search` calls to `filters={"mispar_rechev":"<plate>"}` (CKAN coerces the string to match the numeric column fine — verified directly). Logged as bug-149/150. **Lesson: don't validate a suspected "data doesn't exist" conclusion by re-running the exact same (possibly buggy) query method the app itself uses — that just reproduces the bug as "confirmation." Cross-check with a structurally different query (exact filter vs full-text search) before telling a user their real, everyday vehicle "genuinely isn't in the database."** Also reinforces the existing lesson below: this scraper/lookup chain has no test coverage and no alerting, so silent breakage (external HTML changes, unreliable upstream search indexes) only ever surfaces as a generic "vehicle not found" 404 to end users.

### Database Schema & Migrations

- The `login_log` table (src/lib/login-log.ts) exists in the live Supabase DB but has NO migration file in supabase/migrations — it was created directly in the dashboard. Its `user_id` column is a FK to `users(id)` with no `ON DELETE CASCADE`, unlike `user_roles`/`webauthn_*` which do cascade (see supabase/migrations/20260405_unified_users.sql, 20260424_add_webauthn.sql). Any raw `supabase.from('users').delete()` will 500 with a FK violation for any user who ever logged in, unless `login_log.user_id` is nulled first.
- login_log rows already store full_name/phone independently, so nulling user_id (not deleting the row) is safe — login-report and analytics routes fall back to `full_name` when `user_id` is null.
- No Supabase DB credentials are available in this coding session — new migration `.sql` files under `supabase/migrations/` are never auto-applied. Always tell the user explicitly to run them via the Supabase SQL editor or `supabase db push` after writing one. *(Note: in the 2026-08-19 session, read-only service-role DB access WAS available via `.env.local` — this restriction may not hold in every session; verify env vars before assuming no access.)*
- SQL reserved words used as column names (e.g. `offset`, which collides with `LIMIT...OFFSET`) must be double-quoted in migration files (`ADD COLUMN "offset" NUMERIC`), but Supabase-js/PostgREST calls (`.insert({offset: 5})`) are unaffected — no quoting needed in application code.
- **The live Supabase schema drifts from supabase/migrations/** — confirmed twice in one session (login_log had no migration file at all; user_roles_user_id_fkey's ON DELETE CASCADE in 20260405_unified_users.sql doesn't match the live constraint, which was NO ACTION). Don't trust migration file contents as ground truth for constraint/cascade behavior when debugging FK errors — ask the user to query `information_schema.referential_constraints` directly (query saved in Do-Not-Repeat below), and prefer application-level explicit deletes of dependent rows over relying on ON DELETE CASCADE for any table touching `users`.
- **Bucket creation isn't tracked anywhere in `supabase/migrations/`** — Postgres SQL migrations don't cover Supabase Storage buckets, so a bucket disappearing (project reset, manual dashboard action, etc.) leaves zero trace in version control and zero warning; the only way to notice is a live check (`GET /storage/v1/bucket`) or the silent 500 in server logs when an upload is attempted. Worth a periodic gut-check if any storage-dependent feature is reported as "quietly not working."

### UI Patterns — Responsive/Duplication Risk

- `src/app/admin/vehicles/page.tsx` renders its per-row "⋮" action menu (edit/delete/etc.) in two separate places in the JSX: once for the mobile card view (~line 1618) and once for the desktop table view (~line 1735+, shifts with edits). Any new per-row action must be added to both or it'll silently only work on one viewport. *(Same risk class confirmed manifesting for real in the 2026-08-19 Tracking-tab unification — see UX Audit section below.)*

### Borrow / Return Flow & Signed Forms

- **`src/app/api/wheel-stations/[stationId]/route.ts` GET handler**: the wheels payload only ever exposed a signed form via `current_borrow.form_id`, and `current_borrow` is `undefined` whenever `wheel.is_available` is true (i.e. as soon as a wheel is returned) — so the station page's cards/table views lost all access to a wheel's signed form the moment it was returned, even though the Tracking tab's borrow-history list never had this problem (its own API route fetches all statuses, unfiltered). Fixed by querying `wheel_borrows` for the station across *all* statuses (ordered `created_at desc`, no `.eq('status', ...)` filter) and deriving a separate `last_form_id` per wheel (first form_id seen per wheel_id in the desc-sorted list = most recent) alongside the existing `current_borrow` (still status='borrowed'-only, unchanged, since that also carries borrower PII that shouldn't persist post-return). Frontend now shows the form link whenever `current_borrow?.form_id || last_form_id` exists, in both the wheel-card options menu and a new "טופס" column in the wheels table view.
- [2026-07-26, RESOLVED same session] **`temporarily_unavailable` was never wired into the actual borrow-eligibility checks, only into the manager's own station-page UI.** `is_available` (borrowed vs not) and `temporarily_unavailable` (maintenance/out-of-service) are DELIBERATELY independent flags — `api/admin/analytics/route.ts` and `super-manager/page.tsx` already compute `wheels_borrowed`/`wheels_unavailable` as disjoint sets assuming `is_available` stays `true` while a wheel is merely marked unavailable for maintenance. **Do not "fix" this by flipping `is_available` to false when marking a wheel unavailable — that breaks the existing analytics split.** The real bug was that several consumer surfaces only ever checked `is_available` and never `temporarily_unavailable`: both borrow-creation API routes (`api/wheel-stations/[stationId]/public-borrow/route.ts`, `api/wheel-stations/[stationId]/wheels/[wheelId]/borrow/route.ts`), the public self-service wheel picker (`sign/[stationId]/page.tsx`), and the two vehicle-search pages (`search/page.tsx`, `stations/page.tsx`). `[stationId]/page.tsx` (the manager's own page) already had `!wheel.temporarily_unavailable` guards next to every `is_available` check in its action buttons — strong evidence the original intent was for every consumer to check both fields, it just was never actually done elsewhere. Fixed by adding `temporarily_unavailable` to the select + a 400 guard in both borrow routes, filtering it out in the sign page and both search pages' available-wheel lists, and fixing `[stationId]/page.tsx`'s status pill (card + table) which still showed green "זמין" for a wheel under maintenance despite the correct badge/buttons elsewhere on the same card. Logged as bug-122. *(See also Do-Not-Repeat: this fix initially missed `operator/page.tsx`.)*
- [2026-07-26, RESOLVED same session] **The Supabase Storage bucket `signed-forms` was completely missing from the project** (confirmed via `GET {url}/storage/v1/bucket` on the service-role key returning `[]`), which meant every `form_image_data` upload during `public-borrow` failed silently (`Storage upload error: Bucket not found`) — the outer borrow-creation request still succeeds either way (form upload is fire-and-forget, non-blocking), so `signed_forms` never got a row and `form_id`/`last_form_id` were always `null`, at every borrow status (pending/borrowed/rejected/returned), not just after cancellation/return as the user first suspected. The `signed_forms` table itself was empty too, which looked like "forms were never created," but the user correctly pushed back — the real explanation is `api/signed-forms/cleanup` (a cron job) hard-deletes both the storage file and the DB row for anything past `expires_at`, so if the bucket disappeared (externally — not via any app code path) more than the retention window ago, everything from before would already be auto-purged with no trace, consistent with forms having worked at some point in the past. **Lesson: an empty table is not proof a feature never worked — check for an active cleanup/retention job before concluding "never happened."** Fixed by recreating the bucket via the Storage API directly (`POST {url}/storage/v1/bucket` with the service-role key, `public: false` since the app reads it via `createSignedUrl`, never a public URL) — verified end-to-end on the live test station (submit → reject → view still returns 200 with a valid signed URL). Per user request, retention was also bumped 30→90 days in three places: `api/signed-forms/upload/route.ts` (the `expires_at` calc + the notification-email copy) and `app/forms/[formId]/page.tsx` (the "expired" message), plus the doc-comment in `api/signed-forms/cleanup/route.ts`. Confirmed separately (by re-reading `cleanup/route.ts`): deleting a `signed_forms` row can never cascade-delete the parent `wheel_borrows` row (FKs only cascade parent→child, and the cleanup job only ever touches `signed_forms` + storage) — so borrower name/phone/ID/vehicle/deposit/signature data was never at risk, only the polished full-page image capture. **Confirmed again 2026-08-19** (unrelated audit session, real DB query): any `wheel_borrows` row with `signature_data` set but created *before* ~2026-07-26 20:41 has `signed_forms_count: 0` — i.e. the borrower's raw signature exists on the borrow row, but the polished form image/PDF was never captured and cannot be retroactively regenerated. This is expected/known historical data loss from this incident, not a new bug — don't re-investigate it as one if it comes up again.
- [2026-07-26, found while building the mount-feedback feature] **`handleManualBorrow`'s POST to `.../wheels/[wheelId]/borrow` always sent `vehicle_model`/`vehicle_plate`, but the POST handler never read or stored them** — every manually-entered borrow (the "הזן השאלה ידנית" flow) silently lost the vehicle info, unlike the public sign-form flow. Fixed alongside the mount-feedback feature (logged as bug-137) since the new history view would otherwise show blank vehicles for every manual borrow.
- [2026-07-26, found while building the mount-feedback feature] **The return route's `verified_wheel_matches` upsert ran unconditionally on any return with a license plate, regardless of whether the mount actually worked** — meaning a *failed* mount would still get cached as a "verified compatible" pairing (`verified_by: 'system'`) for future fitment lookups elsewhere. Guarded with `mount_result === 'success'` in the same edit — this was pre-existing behavior, not something introduced by the new feature, but directly contradicted the feature's purpose so it was fixed in place rather than left.
- [2026-07-26] **Per-wheel mount-outcome tracking added.** New nullable columns on `wheel_borrows` (migration `supabase/migrations/20260726_add_wheel_mount_feedback.sql`, user must run manually — no DB creds this session): `mount_result` ('success'|'failed'), `mount_feedback_note` (≤200 chars, doubles as the required failure reason and the optional success note — one field, not two, since only one applies at a time), `mount_feedback_at`, `mount_feedback_by_manager_id`. Captured in the existing return flow (`PUT .../wheels/[wheelId]/borrow`, now requires `mount_result` + conditionally `mount_note` in the body) via a new confirmation modal (`showReturnModal`/`returnForm` in `src/app/[stationId]/page.tsx`) that replaced the old one-click `showConfirm` return. New `GET .../wheels/[wheelId]/history/route.ts` returns a wheel's past requests (date, vehicle_model, license_plate, status, mount_result, mount_feedback_note) — rendered as plain lines (not a table) in its **own dedicated modal** (`showWheelHistoryModal`/`historyWheel`), opened via a new "היסטוריית שאלות" item in the wheel's "⋮" options-menu dropdown. **Not** inside the "ערוך גלגל" (edit wheel) modal — user explicitly corrected this after the first pass put it there, since history-viewing should be reachable regardless of the wheel's borrowed state (edit is disabled while borrowed) and is conceptually a separate action from editing the wheel's specs. Also reused to compute the "this wheel failed on a similar vehicle before" warning shown on pending Tracking-tab rows and in the manual-borrow modal (`findSimilarFailedMount`/`normalizeVehicleText` in `src/lib/vehicle-mappings.ts`). Similarity matching is deliberately plain normalized-text contains-matching on the free-text `vehicle_model` field (user chose this over plate-resolved make/model/year matching, to avoid extra lookup calls and since `vehicle_model` is the one field every request — public form or manual — always has). Client-side history fetches are cached per wheel_id in `wheelHistoryCache` state to avoid refetching for every pending row/modal open.

### WhatsApp Messaging

- [2026-07-26] **Per-manager WhatsApp message wording added.** New nullable `users.whatsapp_message_template` column (migration `supabase/migrations/20260726_add_manager_whatsapp_template.sql` — user must run it manually, no DB creds this session). Editable self-service by any manager (no `is_primary` gate). New `PATCH /api/wheel-stations/[stationId]/auth` endpoint (reuses `verifyStationManager` from `@/lib/station-auth`, same phone+password self-auth pattern as the existing `PUT` password-change handler) persists it. `GET`/`POST` on that same route now also return `whatsapp_message_template` on the `manager` object so it's available on login and session-token verification. Frontend: `DEFAULT_WHATSAPP_TEMPLATE` (module-level const, `{link}` placeholder) + `buildWhatsAppMessage(formUrl)` helper in `[stationId]/page.tsx` — falls back to appending the link at the end if a manager's custom text doesn't contain `{link}`, so a saved template can never break the actual link. Both `sendWhatsAppLink` and `copyWhatsAppText` (previously two copies of the same hardcoded message) now call this one helper. **UI home changed after initial merge**: originally placed inside the "שינוי סיסמא אישית" (`showChangePasswordModal`) modal — a follow-up commit (429e148, merged via PR #5 straight to `main` from a separate `claude/custom-message-before-wheel-ual3dr` session/branch, not done in this session) moved it into its own `showWhatsAppTemplateModal` with a dedicated `AppHeader` profile-menu entry (`?action=whatsappTemplate`) and a real cancel button (dropped the old "reset to default" button, since cancel now serves that purpose better). Same underlying state/handler (`whatsappTemplateForm`, `handleSaveWhatsAppTemplate`, `DEFAULT_WHATSAPP_TEMPLATE`) — no duplication, just relocated JSX. **Lesson reinforced**: this repo gets edited concurrently by other Claude sessions/PRs outside this one's awareness — always `git fetch`/check `origin/main` before assuming local HEAD reflects the deployed state, especially right after pushing.
- [2026-07-27, RESOLVED same session] **The per-manager `whatsapp_message_template` feature (bug-130) only covered the station page's own "שלח קישור בוואטסאפ" button — the call-center/operator page (`/operator`) has a completely separate message-composition flow (`getMessage()`, picking a "contact" from `station.managers` to build a message for coordinating pickup) that was still 100% hardcoded and didn't even fetch the field.** `api/wheel-stations/managers/route.ts` (the endpoint that feeds `station.managers` on the operator page) never selected `whatsapp_message_template` in the first place. Fixed by adding it to that route's select + response, adding it to the `StationManager` type in `operator/page.tsx`, and rewriting `getMessage()` to use `contact.whatsapp_message_template` (with the same `{link}` placeholder + fallback-append convention as `buildWhatsAppMessage` in `[stationId]/page.tsx`) instead of the fixed "תפתח קריאה שינוע לפנצ'ריה..." text — confirmed by the user that the ENTIRE message, not just the link line, should reflect the manager's personal wording. **Lesson: "per-manager custom X" features that surface anywhere a manager's contact info is shown to a third party (not just their own dashboard) need to be audited across every such surface — this app already had two independent message-composition flows for what looks like the same concept to a user.** Logged as bug-142.
- [2026-08-18] **`operator/page.tsx`'s `getMessage()` default pickup-message template now says "איסוף מתחנת גלגלים" (not "צמיגים") and always embeds `מספר גלגל: {wheelNumber}`.** Extended the existing `{link}` placeholder convention (see entry above): a manager's custom `whatsapp_message_template` can opt into an explicit `{wheelNumber}` placeholder to control its position, but if the placeholder is absent the wheel number is now force-appended to custom templates too — so the driver/כונן can always identify the exact wheel from the message text alone, regardless of station wording. User explicitly confirmed this "always force it, even into custom templates" behavior when asked (see Decision Log).

### Sessions & Authentication

- [2026-08-18] **Station-manager password recovery does NOT require printing the QR recovery certificate** — a saved photo/screenshot of it works fine (the flow just scans an uploaded image via `jsQR`, no physical paper requirement). The deliberate fallback if that image is also lost is manual admin contact — not a gap to fill with SMS OTP. User corrected this directly after a UX-audit report mischaracterized the flow as "print-dependent" and proposed SMS recovery as a fix; SMS was rejected specifically due to per-message cost. Don't recommend SMS-based recovery for this flow again.
- [2026-08-19, RESOLVED — see UX Audit section] **The legacy `wheel_manager_{stationId}` session format has been fully removed from the codebase.** All station-manager login/logout/password-change/profile-update code now reads/writes only `station_session_{stationId}`. Don't reference or reintroduce the old format — see the UX Audit section below for what changed and why.

### WebAuthn / Biometric Login

- [2026-08-19] **WebAuthn (Face ID/fingerprint) login: backend fully built, frontend never wired — paused, not abandoned.** `webauthn_credentials`/`webauthn_challenges` tables exist (migration `20260424_add_webauthn.sql`, cascade FKs), `src/lib/webauthn.ts` + all 7 API routes (`api/auth/webauthn/{register,authenticate}/{begin,complete}`, `.../credentials`, `.../credentials/[credentialId]`, `.../status`) are implemented with `@simplewebauthn/server` ^13.3.0. `@simplewebauthn/browser` ^13.3.0 is installed but confirmed (full-repo grep) **never imported anywhere in `src/`** — no page calls `startRegistration`/`startAuthentication` or any webauthn API route, so it was never usable end-to-end. On top of that, the user confirmed the specific UX problem from the prior attempt: it required going through the OS's **Passkey** UI ("מפתחות גישה") instead of a direct app-native biometric prompt. **Root cause confirmed in code:** `register/begin/route.ts` (~line 59-62) sets `authenticatorSelection: { residentKey: 'required', userVerification: 'required' }` — `residentKey: 'required'` forces a discoverable credential, which iOS/Android surface as a synced Passkey (iCloud Keychain/Google Password Manager) via the system Passkey sheet, not a quiet in-app Face ID unlock. `authenticate/begin/route.ts` matches this: when called with no `phone`, it omits `allowCredentials` entirely (relies on OS discoverable-credential lookup) — internally consistent with `register/begin`, but both commit to the Passkey UX. **If resuming:** likely fix is `residentKey: 'discouraged'` in `register/begin` + always passing `phone` to `authenticate/begin` (uses `allowCredentials` instead of OS-level lookup) to get a device-bound, app-scoped credential instead of a synced Passkey. Then still need to build the missing client UI (enrollment in profile/settings, biometric login option on the login page) using `@simplewebauthn/browser`'s `startRegistration`/`startAuthentication`. **Decision already made — see Decision Log:** non-resident credentials confirmed, don't re-litigate.

### Git Workflow & Concurrent Editing

- [FIXED 2026-07-02] `src/lib/version.ts` VERSION used to be auto-bumped by a local-only `.git/hooks/pre-commit` (untracked, machine-local) with a string-replace bug (`m.replace(z, +z+1)` replaced the first textual occurrence of the patch digit(s) anywhere in "VERSION = 'X.Y.Z'", not the patch position — so when major/minor/patch shared a digit, e.g. `4.4.4`, it silently bumped the wrong segment). Fixed by removing that hook and wiring the correct, already-existing `scripts/bump-version.js` (proper 0-9 rollover) through husky instead: `"prepare": "husky"` in package.json + `.husky/pre-commit` runs `node scripts/bump-version.js && git add src/lib/version.ts`. This is now tracked in git, so it runs on every clone after `npm install` (unlike the old raw git hook). Verified working end-to-end via a real commit (18.0.3 → 18.0.4).
- [2026-07-02] A GitHub PR merge + `git pull origin main` + branch checkout happened concurrently while a coding session had uncommitted work on `claude/universal-wheel-integration-sojrdl` — git auto-stashed the uncommitted work (twice, under different messages) before switching branches, rather than losing it. Lesson: if files unexpectedly appear reverted mid-session with no error, check `git stash list` and `git reflog` before assuming work is lost — it's very likely sitting in a stash. `git stash apply` can still fail on unrelated bystander files (e.g. `.wolf/*` auto-log files touched by hooks after the stash was created); resolve those via `git checkout -- <file>` immediately before retrying the apply (hooks may re-dirty them between attempts, so chain checkout+apply in one shell call).
- [2026-07-02/03] Confirmed again, different manifestation: the user actively edits this same repo from their own IDE while a coding session is running (in this case on `main` directly, not a feature branch). Mid-session, the branch pointer moved from `claude/universal-wheel-integration-sojrdl` to `main`, new commits appeared with the user's own unrelated work (ET/offset field, fitment-row fixes), and this session's own uncommitted working-tree edits ended up bundled into one of the user's commits (likely a broad `git commit -a`/"stage all" from their IDE) — `git status` showed the file as clean post-hoc even though it had never been committed via this session's own tool calls. Lesson: don't assume `git status`/`git diff` reflect only this session's work — always check `git log -1 --format='%H %ci' -- <file>` and re-read branch/HEAD state before concluding whether a change needs committing, especially after any long gap (dev-server debugging, scheduled wakeups) where real wall-clock time may have passed.
- [2026-07-29] **Confirmed again (3rd time — see the 2026-07-02 and 2026-07-26 entries above): local `main` sat 5 commits behind `origin/main`** because the user merges PRs (#6, #7 — wheel-count/filter fixes) directly on GitHub without ever pulling locally in this session. User initially thought these were unread PR review "comments" needing action; there were none — the PRs were already fully merged upstream, just not fast-forwarded locally. **At the start of any session where the user references GitHub activity (PRs, comments, "what's on Vercel"), run `git fetch` + compare `main` vs `origin/main` before investigating further** — cheaper than searching PR comments/reviews/notifications for something that turns out to just be an unpulled merge.

### Dev Environment Quirks & Tooling

- **Playwright/Turbopack dev-server quirk (this project)**: navigating headless Chromium directly to `/[stationId]` (a huge ~93k-token route) causes the page's own `fetchStation()` `useEffect` fetch to fail with `net::ERR_ABORTED`, every time, even after long warm-up waits and page reloads — confirmed NOT an app bug: the exact same `/api/wheel-stations/[id]` endpoint returns 200 via `curl`, and via a manual `page.evaluate(() => fetch(...))` issued a few seconds into the same page's lifetime. Root cause not fully pinned down (likely Turbopack HMR settling or a service-worker controllerchange reload racing the effect's very-early fetch) but it reproduces on an unmodified route too, so don't burn time chasing it as if it were a regression — note it to the user as an unverified-by-automation limitation and move on. **2026-08-19 addendum:** a direct `curl` (no browser) against a freshly-started `next dev` can also time out on this specific huge route on first request — retrying with a longer timeout (60s) succeeded in 0.13s, confirming it's a one-time slow first-compile of the route, not a real hang. Don't treat one slow/failed first request to `/[stationId]` as a regression; retry before concluding something broke.
- [2026-07-26] **Broader manifestation of the quirk above, confirmed via plain `curl` (no browser involved) against a locally-started `npm run dev`**: any route nested two dynamic segments deep under `/api/wheel-stations/[stationId]/wheels/[wheelId]/*` (e.g. `.../borrow`, `.../restore`, and the new `.../history`) returns Next's own HTML 404-page for every HTTP method, including methods the route file *does* export (a plain `GET` on the new `history/route.ts`, which only exports `GET`, still 404s) — while the shallower `/api/wheel-stations/[stationId]` and `/api/wheel-stations/[stationId]/wheels` resolve fine (200). Confirmed this is not code-specific: an untouched pre-existing sibling route (`restore/route.ts`) 404s identically to the brand-new `history/route.ts`. `next dev`'s own startup warning ("detected multiple lockfiles... selected the directory of C:\Users\יאיר קדוש\package-lock.json as the root directory") is the likely cause — Turbopack may be watching/serving from the wrong workspace root, which would explain why only the deepest route segments silently fail to register. Don't treat a 404 on these deep nested API paths as evidence of a bug in new code — verify via a direct Supabase query instead (service-role key in `.env.local` works fine for read-only checks) and note the routing gap to the user rather than debugging it as a regression.
- [2026-08-19] **Tooling gotcha, not an app bug:** `curl -d '{"field":"עברית"}'` invoked via the Bash tool on this Windows/Git-Bash setup silently corrupts Hebrew text before it reaches the server (confirmed by reading the saved DB row back — garbled). This is purely a shell/curl argument-encoding artifact on this machine, unrelated to how the real app submits forms (browser `fetch()` + `JSON.stringify()` always sends correct UTF-8). When posting test data with Hebrew content to a local API route from this environment, use a small Node script with `fetch()` instead of inline `curl -d`.

### UX Audit — 2026-08-19 Session (report corrections + fixes shipped)

A full code-based UX audit (all 8 user personas) was conducted this session; findings and decisions are tracked in `UX_AUDIT_TODO.md` at the project root, not duplicated here. Key corrections to that audit worth remembering independently:

- **`src/app/super-manager/page.tsx` has TWO separate tab systems that are NOT duplicate/dead state despite looking like it at a glance: `activeTab` ('wheels'|'borrows', lines ~100, 521-547) controls the two tabs inside the per-station drill-down view (`if (selectedStation)` block, ~line 501+) — the district manager's district-wide view. They coincidentally both have a "borrows"-labeled tab, which reads as leftover-refactor duplication from the code alone, but both are live and render different screens.** A UX-audit report initially flagged `activeTab` as dead state; verified wrong by reading the actual render branches before acting on it. Don't recommend removing `activeTab` here.
- **`src/app/stations/page.tsx` is NOT public despite looking like a browsable directory** — its auth-check `useEffect` (lines 124-138) redirects to `/login` if none of `station_session_*`, `operator_session`, or legacy `wheel_manager_*` exist in localStorage, exactly like `/search` does. A UX-audit report (and the Explore-agent research behind it) incorrectly described it as "public, no forced auth" — only the separate `vehicle_db_manager` login (for editing vehicle-model DB entries) was actually auth-gated in that description; the whole page requires a station-manager/operator session. Of the app's 4 "find a compatible wheel" tools, only `/lookup` and `/reverse-search` are genuinely public; the `/stations` lookup modal and `/search` both require staff login. Don't repeat the "stations is public" claim.
- **`wheel_manager_{stationId}` legacy session format fully removed** (implementation): 7 sites in `[stationId]/page.tsx` + `AppHeader.tsx` + `search/page.tsx` + `stations/page.tsx` + Storybook mocks. The station-manager login modal survives only for the password-recovery "log back in" step, and now writes the modern `station_session_` format. This also surfaced and removed two now-orphaned pieces of dead code: the `GET` token-verification handler + `verifyToken`/`SESSION_EXPIRY_MS` on `api/wheel-stations/[stationId]/auth/route.ts`, and the standalone `api/super-manager/auth/route.ts` (its underlying `verifySuperManager` function is still very much in use elsewhere, for step-up auth on sensitive wheel operations — only the unused HTTP route was removed).
- **Confirmed the exact same responsive-duplication risk pattern (see "UI Patterns" above) manifesting for real**: the station page's Tracking tab had a desktop `<table>` and a separate mobile-cards `<div>`, and the mobile version had actually drifted — it was missing a quick "approve" button that the desktop table had (mobile only offered a combined "preview and approve" that always opened a modal first). Unified into one responsive CSS-grid card list; the missing button was restored for all screen sizes. Verified live via a real test borrow created at station id `588e5455-3b5d-4090-9d11-4adbd7c9bbcb` ("תחנה לבדיקה").
- [2026-08-19] **`src/app/sign/[stationId]/page.tsx` rebuilt from a single-page form into a 3-step wizard (זהות ← רכב/ערבון ← תנאים+חתימה)**, per an explicit UX-audit TODO decision. Key technical points worth remembering if this file is touched again: (1) the form's `html2canvas` capture (the ONLY usage of html2canvas in this codebase) — used to save a "legal record" image viewable via `/forms/[formId]` — now targets a separate, always-mounted, **off-screen** node (`position: absolute; left: -10000px; width: 640px` — NOT `display:none`, which html2canvas skips entirely) that independently re-renders a full summary from component state, instead of the visible form. This was necessary because a wizard only shows one step's DOM at a time, and the old single `formRef` approach would have silently dropped steps 1-2's data from every future saved record. (2) **Discovered + fixed a real pre-existing bug in the process**: the visible terms box (`maxHeight:200px, overflowY:auto`) is scrolled to the bottom at submit time (required to unlock the agree-checkbox) — `html2canvas` captures overflow containers as currently scrolled, not full `scrollHeight`, so every legal-record image saved *before* this fix almost certainly shows only the terms' last clause, not the full 5-clause list. The new hidden capture node renders the terms unclipped. (3) The 3 step-blocks stay permanently mounted (`display:none` toggling, not conditional unmount) specifically so the signature `<canvas>` (which only lives in step 3) never gets destroyed/recreated by back-navigation — losing its pixels while `hasSigned` stayed `true` would have let a user resubmit with a blank-looking (but validation-passing) signature box. Signature is now also mirrored into a `signatureDataUrl` state (set in `stopDrawing`, cleared in `clearSignature`) so both the hidden capture node and final submission read from state, not a live canvas query. (4) The canvas-setup `useEffect` must include `currentStep` in its deps — `getBoundingClientRect()` on a `display:none` ancestor returns a zero-size rect, so the canvas would otherwise get sized 0×150 before the user ever reaches step 3. **Verified live via Playwright** (not just `tsc`) — filled all 3 steps, went back and forward, submitted for real, and inspected the actual saved `form_image_data` PNG to confirm all fields + the full unclipped terms text + the signature all appear correctly. **Follow-up fixes same day, both user-reported after live testing**: (a) the canvas-resize `useEffect` (added to fix a different bug — zero-size canvas on first reaching step 3) unconditionally did `canvas.width = rect.width` on every run; **setting `canvas.width`/`height` always clears the canvas bitmap even if the value is unchanged** — so every "back" navigation to step 3 silently wiped the drawn signature (leaving a blank box, no placeholder text either since `hasSigned` stayed true). Fixed by only resizing when the size actually changed, and redrawing from `signatureDataUrl` when it does. **General lesson: never assign `canvas.width`/`canvas.height` inside an effect that can re-run for reasons unrelated to an actual size change — always guard with a comparison first.** (b) The submit-spinner and success-checkmark icons weren't centered — `<svg>` wrapped in a plain div with only `marginBottom` doesn't reliably center via an ancestor's `text-align:center`; this file's own established convention (`<span style={{display:'inline-flex',alignItems:'center',gap:'4px'}}><svg.../></span>`, used everywhere else in this file) exists precisely because bare SVGs need explicit flex centering. **Convention to reuse: any standalone/large SVG icon wrapper in this file needs `display:'flex', justifyContent:'center'` (or the inline-flex span pattern for inline icons) — never rely on `text-align` alone.** See `UX_AUDIT_TODO.md` for the full writeup.
- [2026-08-19] **Station deposit type "ID/license document" (`payment_methods.id_deposit`/`license_deposit`) flipped from default-on to default-off, at explicit user request.** Discovered inconsistency: the manager-facing settings checkboxes (`src/app/[stationId]/page.tsx` ~line 4843) already visually showed *unchecked* for any station that never touched this setting (`editPaymentMethods.id_deposit || false`), while the public sign form (`sign/[stationId]/page.tsx`) actually treated undefined as *enabled* (`!== false` check) — so managers were seeing "off" in their own settings while it was live for real borrowers. Fixed both sides: the public form's check flipped to `=== true` (opt-in), and the two settings-modal default fallbacks (initial `useState` + the modal-open fallback) flipped from `id_deposit: true, license_deposit: true` to `false, false`. Also: when **both** are enabled for a station, the public form now shows them merged into one visual row ("מסמך מזהה כפיקדון") with two sub-choices, instead of two separate full rows — but `deposit_type` submitted is still distinctly `'id'` or `'license'` under the hood, since the station manager's page (`[stationId]/page.tsx`) displays/relies on that distinction in multiple places (borrow history, return reminders) to know which physical document to hand back. Don't collapse these into a single combined value if this area is touched again. Also added `isValidIsraeliId()` (official Israeli teudat-zehut check-digit algorithm) as a hard-blocking validation on the identity step — rejects structurally-impossible ID numbers (e.g. random digits) but is not and cannot be an identity-existence check.
- [2026-08-19] **Three pending TODO items resolved to decisions:** (1) `admin/vehicles` year-range filter bug (`page.tsx:1274-1279`, only checks `year_from` against both bounds, ignores `year_to`) — user re-confirmed **deliberately deferred, do not fix**. (2) Accessibility widget — user chose **build custom, not a ready-made service (UserWay/נגיש לי)**; scope/coverage of תקן 5568 not yet planned, needs its own scoping conversation before implementation. (3) "הערות/הודעות בממשק" turned out to mean the unexplained "CB" abbreviation in `/search`'s wheel-result cards — fixed by matching the existing filter-label convention `"CB (קוטר מרכז)"` (was just `"CB {value}"`) in both `resultWheelSpecs` render sites in `search/page.tsx`, plus bumped that row's font-size 0.8rem→0.85rem and added `flexWrap` so the longer text doesn't overflow the card. **Established convention: this codebase already spells center-bore as `"CB (קוטר מרכז)"` (see the filter dropdown label) — reuse that exact phrasing anywhere CB is shown to a user, don't invent new wording.**

- [2026-08-20] **Role-switching logic (5 near-identical copies: `AppHeader.tsx`, `operator/page.tsx`, `call-center/page.tsx`, `AdminSidebar.tsx`, `login/page.tsx`'s post-auth role picker) consolidated into `src/hooks/useRoleSwitch.ts`.** The auth/role data model: one phone+password login can resolve to multiple `RoleResult[]` (station_manager/operator/district_manager/editor/admin — see `src/lib/types.ts`, moved there from `api/auth/login/route.ts` where it was previously defined and hand-redeclared in 2 of the 5 files instead of imported). "Switching roles" = writing a fresh session blob to a role-specific localStorage key (`station_session_*`, `operator_session`, `super_manager_session`, `puncture_manager_auth`, `wheels_admin_auth`) plus `active_role`/`active_sub_role`, then navigating — there is no shared React state across roles, each is a fully separate legacy-style session. `useRoleSwitch()` exposes `{authRoles, activeRole, activeSubRole, activeRoleEntry, currentRoleLabel, switchToRole}`; deliberately does NOT manage menu open/close UI state (`showRoleMenu` etc. stay local per-file since the UI differs: header dropdown vs sidebar list vs full-screen picker cards) — keep that boundary if extending this hook. **`AppHeader.tsx` is the one exception that could NOT be fully absorbed**: it has a unique fallback-reconstruction path (derives a synthetic 1-entry `authRoles` from whichever individual session key exists, when `auth_roles` itself is missing/legacy) intertwined with its own `userSession`/`isLoading` state — that logic was left in place untouched, and `AppHeader` only adopts the hook's `switchToRole` for the actual session-write+navigate step (still calls its own local `setActiveRole`/`setShowRoleMenu` alongside it for the same-render UI feedback, since `router.push` doesn't synchronously unmount the current page). Verified live via Playwright against a real (temporary, deleted after) 2-role DB test user — see buglog entries bug-189 through bug-192 for the 4 real pre-existing bugs this surfaced and fixed.
- [2026-08-20] **WebAuthn (biometric login) frontend wired up — see [[webauthn_biometric_login_paused]] for the full prior investigation.** Backend was 100% pre-built (DB tables, `src/lib/webauthn.ts`, 7 API routes under `api/auth/webauthn/*`) but had zero frontend usage anywhere. Root cause of the prior "didn't work well" attempt, confirmed in code: `register/begin/route.ts` used `residentKey: 'required'`, forcing a synced OS Passkey UX (iCloud Keychain/Google picker) instead of a direct device-bound fingerprint prompt — fixed to `'discouraged'` per an already-confirmed decision from the prior investigation. Deliberately scoped the whole feature to live only in `src/app/login/page.tsx` (an enroll link + a conditional biometric-login button, both gated by `browserSupportsWebAuthn()` and a debounced `GET .../status?phone=` check) rather than spreading it into every role's separate settings UI, since this app has no single unified profile/settings screen. `authenticate/complete` already returns the exact same `{success, roles: RoleResult[]}` shape as `/api/auth/login`, so it plugs straight into the existing `proceedWithRoles()`/`useRoleSwitch().switchToRole()` pipeline with no changes needed there. **Known gap intentionally left alone**: `switchToRole` embeds `localStorage.getItem('auth_password')` into every session it writes; a passkey-only login has no real password to give it (stored as `''`), so a later "change password" flow that compares against the session's cached password (e.g. `call-center/page.tsx`) will reject until that user logs in with a password at least once. Pre-existing plaintext-password-in-localStorage architecture, not something this feature should try to redesign. **Security gap found and fixed same day, after flagging it to the user and getting explicit go-ahead**: `register/begin/route.ts`'s password check was `if (password && user.password)` — if password was omitted or the user had none stored, it silently skipped verification and issued registration options for just a phone number, no real identity proof (anyone knowing only a victim's phone number could register their own passkey on that account, by calling the API directly instead of going through the UI). Fixed to unconditionally require and verify the password (reject with 401 if either the request has no password or the user has none stored). Doesn't affect day-to-day biometric login at all (`authenticate/*` never touched passwords) — only the one-time enrollment step. Verified via direct curl calls: no-password and wrong-password requests now correctly rejected, correct password still works. **Verified live** with a real WebAuthn ceremony (not mocked) via Chrome DevTools Protocol's virtual authenticator (`WebAuthn.addVirtualAuthenticator`, `protocol: 'ctap2', transport: 'internal', isUserVerified: true` — simulates a platform fingerprint reader, but still exercises the real `@simplewebauthn/server` crypto verification): enroll → success toast → login button appears → biometric-only login → correct role navigation. Real device testing (actual iPhone/Android Face ID or fingerprint) was NOT done and still needs the user to verify — CDP simulation is not a substitute for that.
- [2026-08-21] **"Forgot password via WhatsApp" implemented per [[project_forgot_password_whatsapp_idea]]'s design — added as a second tab inside the existing forgot-password modal in `src/app/login/page.tsx`, not a replacement for the QR-recovery-certificate flow.** New `forgotMethod: 'qr' | 'whatsapp'` state toggles between them; the QR path's JSX/logic is untouched. WhatsApp path: full name (required, new `forgotFullName` state) + phone (reuses existing `forgotPhone`) + optional requested password (reuses existing `forgotNewPassword`, no confirm-field needed since it's just a note to a human, not an actual live password change) → builds a plain-text message and opens `https://wa.me/{owner's number}?text=...` in a new tab, following the exact URL-building convention already used elsewhere in this codebase (`buildWhatsAppMessage`/`sendWhatsAppLink` in `src/app/[stationId]/page.tsx`: `international = clean.startsWith('0') ? '972' + clean.slice(1) : clean`). **The app owner's WhatsApp number (0548461289) is now a hardcoded constant (`OWNER_WHATSAPP_PHONE`) in `login/page.tsx`, committed to source control** — matches what the original design doc asked for ("as a constant"), but worth knowing it's in plain sight in the repo, not an env var, if that repo's visibility ever changes. Verified live via Playwright: message content (name/phone/password) and target number confirmed correct by decoding the actual popup URL's query string, validation blocks empty required fields, and the pre-existing QR tab still renders/behaves identically (no regression).

- [2026-08-21] **"בדוק סטטוס בקשה" for operators (`operator/page.tsx`) required first fixing a real data-loss gap: sending a wheel link had zero persistence — `closeModal()` wiped `selectedWheel`/`selectedContact` with nothing surviving anywhere.** User explicitly chose the full server-side design (new `operator_sent_requests` table + migration) over a no-migration alternative that would've reused the existing `signed_forms.referred_by`/`ref=operator_<id>` mechanism — that simpler option couldn't distinguish "link sent, not yet filled" from "no record at all," and wasn't visible to anyone but the sending operator. **Migration `supabase/migrations/20260821_add_operator_sent_requests.sql` is NOT YET APPLIED** — this session has no DDL access (`supabase projects list` showed only unrelated projects under the logged-in CLI account; linking blindly would risk targeting the wrong project) — user must run it via the Supabase SQL editor before the feature does anything beyond returning empty/failing gracefully. **Deliberately did not touch the existing `ref=operator_<id>` param** (shared across all of one operator's sends, can't disambiguate between them, and already load-bearing for `api/call-center/history`'s exact-match `referred_by IN (...)` filter) — added a second, independent `sr=<operator_sent_requests.id>` param instead, unique per send, threaded through `sign/[stationId]/page.tsx` → `public-borrow` → a new nullable `wheel_borrows.operator_send_request_id` column. This is the sole join key for status checks; no need to correlate by wheel/station identifiers at all. **The send-log row is created lazily** — on the operator's first actual copy/share click for a wheel, not when the modal merely opens — specifically to avoid phantom log rows for modals opened and abandoned; the resulting id is threaded through `getMessage(srOverride?)`/`getFormLink(srOverride?)` as an explicit override parameter rather than read from state, because `copyMessage`/`copyLinkForDriver` need the *freshly-created* id in the same async call before a state update could ever be reflected back into the same execution. **Verified live that the entire existing operator flow (search, modal, copy) keeps working with zero visible degradation even before the migration exists** — the new fetch/log calls 500 in the background, caught and logged quietly, falling back to the pre-existing link format (no `sr`) rather than blocking anything. **Cross-session note**: a separate planning document (`UX_AUDIT_TODO.md`'s "ממשוב מוקדנים/מנהל מוקד" section, full plan at `.claude/plans/giggly-wondering-stroustrup.md`) was found already referencing this exact table/function (`operator_sent_requests`, `ensureSendRequestId()`) as a dependency for a future "one-click WhatsApp contact" feature — that other work should not start until the migration here is actually applied.
- [2026-08-19] **Reused the light-theme convention check the hard way**: on `operator/page.tsx`'s new "sent requests" list, first drafted with dark-theme colors (light-gray text on near-transparent dark backgrounds) by pattern-matching a different assumption about this file, then caught it by actually reading `styles.pageWrapper`/`styles.section` (`background: '#f1f5f9'`/`'#ffffff'`, `color: '#1e293b'`) before shipping — this file is light-themed like most of the app. **Always read the actual existing `styles` object's base colors for a file before choosing new inline colors, never assume dark-mode by default in this codebase.**

## Conventions

- **`src/app/punctures/page.tsx` (+ `src/components/punctures/MapView.tsx`, `HoursFields.tsx`) is the project's reference implementation for UI quality.** It's the newest page and the only one using real Tailwind classes instead of hand-rolled `style={{}}` objects, the only place with a genuinely shared/reused component (`HoursFields`, used by both the public page and the admin editor), correct handling of tricky edge cases (overnight hour ranges, Friday/Saturday special hours, 24/7 shorthand, geolocation permission-denied), a clean single responsive toggle (list/map) instead of duplicated `isMobile`-branch JSX, and distinct/specific empty-state copy per situation rather than one generic message. **When refactoring or rebuilding any other page in this app (station, operator, search, stations, admin/vehicles, super-manager, etc.), default to copying its patterns instead of inventing new ones** — user explicitly confirmed this as a standing principle during a 2026-08-18 UX audit walkthrough, not just a one-off suggestion.

## Do-Not-Repeat

<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->
- [2026-08-22] **Plan-mode plan files (`~/.claude/plans/<random-name>.md`) get their random filename REUSED by later, unrelated plan-mode sessions — the file's content silently changes while `.wolf/anatomy.md`'s cached one-line description does not.** Confirmed directly: anatomy.md described `splendid-dreaming-ullman.md` as "Hook משותף להחלפת תפקיד — useRoleSwitch" (accurate as of the 2026-08-19 scan), but by 2026-08-22 that same filename held a completely different, newer plan ("בדוק סטטוס בקשה למוקדן" / operator sent-request tracking, dated 2026-08-21) — which turned out to already be LIVE in `operator/page.tsx` (`operator_sent_requests` table, `ensureSendRequestId()`, the "בקשות שנשלחו לאחרונה" panel) and materially overlapped with a new plan being drafted for the same file/modal. **When asked to check a new plan against "other plans," don't trust anatomy.md's cached description of files under `~/.claude/plans/` — always read the actual current file content, and cross-check anything that looks WHEELS_APP-relevant (not just skip past files with a stale-sounding name) against the live code with a targeted grep**, since the plan may already be shipped rather than merely "another plan to coordinate with." General lesson: this same `~/.claude/plans/` directory is shared across unrelated projects too (Rina Kadosh e-commerce, a family-tree app, a garage/customer-tracking app all had files there during this check) — filter by actual content, not just assume every file is this project's.
- [2026-07-26] **When fixing "checks is_available but not temporarily_unavailable" (bug-122), I only checked search/page.tsx and stations/page.tsx and declared it done — missed that `src/app/operator/page.tsx` (call-center) runs its OWN third, independent copy of the same vehicle-search-and-render logic (3x duplicated across its plate/model/manual tabs) with the exact same gap.** Lesson: when a bug is "surface X doesn't check field Y", grep the WHOLE repo for the field/pattern (`is_available`) across all consumer pages before declaring the fix complete — don't stop at the 2-3 files you already know about. Fixed as bug-129.
- [2026-07-26] **When adding the per-manager `whatsapp_message_template` field, I only updated `/api/wheel-stations/[stationId]/auth` (the in-station login modal's own endpoint) and missed that the actual primary login flow used in production is the unified `/login` page → `/api/auth/login` → `checkUnifiedUser()`, a completely separate code path that also needed the new column selected and passed through.** On top of that, `[stationId]/page.tsx`'s session-restore effect (`validateSession`) reconstructs `currentManager` from the cached session object by hand-picking 5 named fields — any new field added to `Manager` must also be added there explicitly, or it silently gets dropped on every page load even if the login API returns it correctly. And `handleSaveWhatsAppTemplate` only wrote to the legacy `wheel_manager_{stationId}` localStorage key, not the newer `station_session_{stationId}` key that `/login` actually writes — so even a successful save wouldn't survive a refresh for users on the new flow. Lesson: **this app has two parallel manager-session mechanisms (`wheel_manager_*` / old direct in-page login, and `station_session_*` / unified `/login` page) that both need to be checked whenever manager-session data shape changes.** Fixed as bug-130. *(The `wheel_manager_*` format was fully removed on 2026-08-19 — see UX Audit section — so this specific dual-mechanism risk no longer applies going forward, but the general lesson about grepping ALL session-shape consumers still does.)*
- [2026-07-02] Deleting a user via `supabase.from('users').delete().eq('id', userId)` fails with a 500 ("שגיאה פנימית") whenever the user has login_log rows, because that FK has no CASCADE (see Key Learnings). This also silently broke `/api/admin/users/merge`: it moves roles to keep_id FIRST, then deletes delete_id — when the delete step hit this same FK error, roles had already moved but delete_id survived as a roleless ghost account, which read to the user as "role duplication". Fix: always `update({ user_id: null }).eq('user_id', <id>)` on `login_log` before deleting a row from `users`, in any endpoint that deletes users (both `[userId]/route.ts` and `merge/route.ts` now do this). A DB-level fix (ON DELETE SET NULL on the FK) was added in supabase/migrations/20260702_fix_login_log_user_fk.sql — user confirmed it was applied via Supabase SQL editor.
- [2026-07-02] After fixing login_log, user delete STILL 500'd. User ran a manual DELETE in the Supabase table editor and got the real Postgres error verbatim: `user_roles_user_id_fkey` also has no ON DELETE CASCADE in the live DB, despite 20260405_unified_users.sql defining it that way. Confirmed by querying `information_schema.referential_constraints` — user_roles was `NO ACTION`, while webauthn_credentials/webauthn_challenges were correctly `CASCADE` and login_log was `SET NULL` post-fix. **General lesson: this project's live Supabase schema does not reliably match the migration files in supabase/migrations/ — treat migration file contents (including ON DELETE behavior) as aspirational, not ground truth, when debugging FK errors.** When a delete/cascade bug is suspected, ask the user to run this against `information_schema` rather than trusting the migration files: `SELECT tc.table_name, tc.constraint_name, rc.delete_rule FROM information_schema.table_constraints tc JOIN information_schema.referential_constraints rc ON tc.constraint_name = rc.constraint_name JOIN information_schema.key_column_usage kcu ON kcu.constraint_name = tc.constraint_name WHERE tc.constraint_type = 'FOREIGN KEY' AND EXISTS (SELECT 1 FROM information_schema.constraint_column_usage ccu WHERE ccu.constraint_name = tc.constraint_name AND ccu.table_name = '<target_table>');`. Fix: app code now deletes `user_roles`/`webauthn_credentials`/`webauthn_challenges` explicitly before deleting a user instead of trusting any FK's delete behavior (src/app/api/admin/users/[userId]/route.ts, src/app/api/admin/users/merge/route.ts). DB-level fix in supabase/migrations/20260702_fix_user_roles_user_fk.sql — user confirmed applied ("Success. No rows returned").

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->
- [2026-07-26] **Mount-outcome feedback is collected at return time, not as a separate standalone action.** User confirmed this explicitly when asked — a wheel's borrow/return cycle is already the natural "closing" moment where the manager knows whether it fit, so no new independent workflow was added.
- [2026-07-26] **"Similar vehicle" matching for the failed-mount warning uses simple normalized free-text comparison on `vehicle_model`, not license-plate-resolved make/model/year.** User picked this explicitly over the more precise plate-based option — it needs no extra API calls, works for every request (plate is optional), and ships faster; accepted trade-off is it can miss/false-positive on inconsistent free-text vehicle descriptions.
- [2026-07-26] **The failure reason is free text (≤200 chars), not a picklist of common reasons.** User picked this explicitly — matches the project's existing precedent (no reason-code taxonomy exists anywhere else in the app) and stays flexible for site-specific fitment issues.
- [2026-08-18] **The pickup-message's wheel number must reach the driver even when a station manager has set their own custom `whatsapp_message_template`.** User picked this over "only fix the default template" when asked directly — the goal (a כונן identifying the right wheel from the message alone, without asking anyone) applies regardless of a station's wording preference, so `getMessage()` force-appends "מספר גלגל: X" to custom templates lacking a `{wheelNumber}` placeholder rather than leaving it out.
- [2026-08-19] **WebAuthn credentials should be non-resident (`residentKey: 'discouraged'`), not synced OS Passkeys.** User confirmed when asked: every user in this app has exactly one device, so losing cross-device Passkey portability is a non-issue. Settles the trade-off noted in the entry above — when resuming WebAuthn work, implement `residentKey: 'discouraged'` directly rather than re-asking.
- [2026-08-19] **Feedback-form's "close" button should return to wherever the user came from (`router.back()`), not to `/`.** Found and fixed during the UX audit session — going to the public homepage looked and felt like a forced logout to authenticated users (station managers, operators) even though their session was untouched. The post-*submission* "back to homepage" button was deliberately left as-is (different context — task is complete, not mid-form).
