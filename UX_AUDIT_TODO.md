# סיכום החלטות — ביקורת UX (2026-08-19)

מסמך עבודה שנוצר אחרי מעבר משותף על כל ההמלצות מדוח ביקורת ה-UX (8 סוגי משתמשים + פיצ'רים מוצעים), ומתעדכן תוך כדי היישום בפועל. מאורגן לפי **סטטוס** (מה בוצע / מה פתוח / מה מחכה להחלטה שלך / מה נדחה) כדי שיהיה קל לראות במבט אחד איפה עומדים.

> דוח מקורי (Artifact): https://claude.ai/code/artifact/f964306a-b050-4254-a138-4ac9e2798f60 — מכיל כמה טעויות שתוקנו כאן (ראה "תיקונים לדוח המקורי" בסוף).

---

## ✅ בוצע ואומת (`tsc --noEmit` נקי אחרי כל שינוי)

**שכחתי סיסמה דרך WhatsApp**
- נוסף כאפשרות שנייה במודל "איפוס סיסמא" הקיים בעמוד הלוגין (`login/page.tsx`), לצד תעודת השחזור הקיימת — לא במקומה. שני טאבים: "תעודת שחזור" (ברירת מחדל, הזרימה הקיימת) ו"פנייה בוואטסאפ" (חדש).
- הטאב החדש: שם מלא (חובה) + טלפון (חובה) + סיסמה מבוקשת (אופציונלי) → פותח וואטסאפ עם הודעה מוכנה למספר שלך (`0548461289`, קבוע בקוד לפי מה שביקשת בתכנון המקורי).
- **אומת חי בדפדפן** (Playwright): תוכן ההודעה נכון (שם/טלפון/סיסמה מבוקשת), המספר הנכון בקישור, ולידציה על שדות חסרים, בלי לפגוע בטאב הקיים (תעודת שחזור עדיין עובד זהה למקודם).

**Hook משותף להחלפת תפקיד — `useRoleSwitch`**
- לוגיקת "החלף תפקיד" (שכתבה session חדש ל-localStorage וניווטה למסך המתאים) הייתה מיושמת בנפרד ב-5 מקומות (`AppHeader.tsx`, `operator/page.tsx`, `call-center/page.tsx`, `AdminSidebar.tsx`, ומסך הבחירה הראשוני ב-`login/page.tsx`). אוחדה ל-hook אחד חדש (`src/hooks/useRoleSwitch.ts`) שכל 5 המקומות קוראים לו עכשיו.
- **4 באגים אמיתיים שנמצאו תוך כדי קריאת הקוד (לא רק חשד) ותוקנו כחלק מהאיחוד:**
  1. `AppHeader.tsx` ניווט ל-`'\call-center'` (backslash שגוי, לא `/`) — ב-JS זה בפועל התכווץ למחרוזת `'call-center'` בלי `/` בהתחלה. **אומת בדפדפן שהניווט ל-`/call-center` עובד נכון עכשיו.**
  2. `AdminSidebar.tsx` לא כתב `active_sub_role` בכלל בהחלפת תפקיד — עלול לאבד את ההבחנה מוקדן/מנהל-מוקד בחזרה.
  3. כל 4 העתקי-ה-switcher שמרו session עם תת-קבוצת שדות מצומצמת (`{id, full_name, phone, role, is_primary}`), בעוד ש-login בהתחברות ראשונית שמר את **כל** שדות ה-`data` — שדה עתידי שיתווסף היה נעלם בכל מעבר חוץ מהתחברות.
  4. `district_manager` איבד את `can_edit` בכל 4 ההעתקים (היה רק ב-login).
- אוחד גם מנגנון הניווט: 2 מקומות (מוקדן, מנהל-מוקד) עשו `window.location.href` (רענון מלא), 2 אחרים `router.push` (מעבר חלק) — כולם אוחדו ל-`router.push`, לפי החלטתך.
- בונוס קטן נוסף: `src/hooks/useClickOutside.ts` — hook גנרי חדש שמחליף 3 עותקים כמעט-זהים של "סגור תפריט בלחיצה מחוץ לו".
- **אומת חי בדפדפן** (Playwright, לא רק `tsc`) — נוצר משתמש בדיקה זמני עם 2 תפקידים אמיתיים במסד הנתונים (נמחק בסוף): התחברות → מסך בחירת תפקיד → מנהל תחנה (session עם כל השדות) → החלפה דרך צ'יפ ה-AppHeader למנהל-מוקד (ניווט נכון ל-`/call-center`, `active_sub_role` נשמר נכון) → העמוד נטען עם דאטה אמיתי (לא תקוע) → החלפה בחזרה למנהל-תחנה דרך תפריט עמוד מנהל-המוקד עצמו (מוודא את שינוי `window.location.href`→`router.push`). אפס שגיאות קונסולה אמיתיות.

**מנהל תחנה**
- מנגנון ההתחברות הישן (`wheel_manager_{stationId}`) הוסר לגמרי מכל הקוד — 7 מקומות ב-`[stationId]/page.tsx` + `AppHeader.tsx` + `search/page.tsx` + `stations/page.tsx` + Storybook mocks. חלון ההתחברות עצמו נשאר (לתהליך שחזור סיסמה בלבד), אך כותב עכשיו לפורמט החדש (`station_session_`). בעקבות זה נמצא ונמחק גם `GET`/`verifyToken`/`SESSION_EXPIRY_MS` היתומים ב-`api/wheel-stations/[stationId]/auth/route.ts`.
- טאב "מעקב" אוחד לתצוגת כרטיסים רספונסיבית אחת (CSS Grid `auto-fill`) במקום טבלת-דסקטופ + מובייל-קארדים נפרדים. **בדרך התגלה ותוקן פער פונקציונלי אמיתי:** בגרסת המובייל הישנה חסר כפתור "אשר" מהיר לבקשות ממתינות (רק "בדוק ואשר" משולב), בעוד שבדסקטופ היו 3 כפתורים נפרדים (בדוק/אשר/דחה). **אומת חזותית** ע"י יצירת בקשת השאלה אמיתית בתחנת הבדיקה (`תחנה לבדיקה`) — הכפתורים והמעברי-סטטוס עובדים.

**מוקדן + `/search`**
- חיפוש רכב אחיד: שדה לוחית גדול עם `autoFocus`, בלי צורך ללחוץ טאב. מתחת לשדה — שורת "לא נמצא?" ומתחתיה כפתורי-pill קטנים לחלופות (דגם, ובמוקדן גם מפרט), עם כפתור חזרה כשעוברים לשם. פריסה עודכנה פעמיים לפי משוב חזותי.
- `DEV_NOTES.md` נוצר בשורש הפרויקט עם תזכורת: כל שינוי בכלל זמינות גלגל חייב להיבדק גם ב-`operator/page.tsx` (נשכח בעבר, bug-129).

**מנהל מוקד**
- כותרת ברורה בראש שני המסכים הדומים-בשם: "ניהול {שם המוקד}" (דינמי, לפי מה ששמור) ב-`/call-center`, "ניהול כל המוקדים במערכת" ב-`/admin/call-centers`.

**אדמין**
- מתג "הצג מוצפן" בדף `admin/users` נמחק לגמרי (state + כפתור + לוגיקת סינון).

**מנהל מחוז**
- קובץ הנתיב היתום `src/app/api/super-manager/auth/route.ts` נמחק (אומת: אפס קוראים בכל הפרויקט). `verifySuperManager`/`super-manager-auth.ts` נשארו בדיוק כמו שהיו — הם בשימוש אמיתי במקומות אחרים.

**חיפוש ציבורי**
- `/lookup` הפוך להפניה (client redirect) ל-`/reverse-search` — הקוד הישן שלו הוסר לגמרי. פותר גם את הקישור השבור `/lookup → /search` שהיה בדוח המקורי.
- הודעת ביניים ("צריך להתחבר... מעביר אותך למסך התחברות") במקום מסך לבן/loading-אינסופי, גם ב-`/search` וגם ב-`/stations`.

**Feedback (נמצא תוך כדי השיחה, לא היה בדוח)**
- כפתור "סגור" בטופס Feedback ניווט תמיד ל-`/` (דף הבית הציבורי) במקום לחזור לעמוד שממנו הגעת — הרגיש כמו יציאה מהחשבון גם כשה-session לא נמחק. תוקן ל-`router.back()`.

**`/search` — תוצאות גלגל (הבהרה שביקשת ל"סדר הערות/הודעות")**
- תווית "CB" ליד קוטר המרכז בכרטיסי תוצאות גלגל (גם בחיפוש לפי מפרט וגם בתוצאות לפי רכב) לא הוסברה — עודכנה ל-"CB {ערך} (קוטר מרכז)", תואם לניסוח שכבר קיים בתווית הפילטר. גם הועלה גודל הפונט של שורת המפרט (0.8rem → 0.85rem) והוסף `flexWrap` כדי שהטקסט הארוך יותר לא ישבור את הכרטיס. שני המקומות ב-`search/page.tsx` (רשימת גלגלים לפי תחנה + רשימת גלגלים לפי רכב) עודכנו.

**`/sign/[stationId]` — טופס החתימה הציבורי**
- פוצל מעמוד גלילה ארוך אחד ל-wizard תלת-שלבי: **זהות ← רכב/ערבון ← תנאים+חתימה**, עם מחוון התקדמות ("שלב X מתוך 3"), אימות לפי שלב (שדות חסרים מודגשים באדום + toast, לא ניתן להתקדם בלי מילוי), וכפתור "חזרה" ששומר את כל הנתונים שהוזנו. שלושת השלבים נשארים תמיד מורכבים ב-DOM (`display:none` כשלא פעילים, לא נהרסים) — כדי שלוח החתימה לא ייראה ריק אחרי "חזרה" וחזרה קדימה.
- **תוקן אגב הפיצול, לא היה מתועד בדוח:** התמונה שנשמרת כרשומה משפטית/ראייתית (`html2canvas`, נצפית ב-`/forms/[formId]`) עברה לצלם node נפרד ומוסתר (מחוץ למסך, רוחב קבוע 640px) שבונה תקציר מלא מה-state בלי תלות באיזה שלב מוצג — פותר את הבעיה שצילום המסך הנראה בסוף שלב 3 היה מפספס לגמרי את הפרטים משלבים 1-2. **בדרך התגלה ותוקן באג אמיתי נוסף:** תיבת התקנון (`maxHeight:200px` עם גלילה) נתפסה ע"י `html2canvas` לפי מיקום הגלילה בפועל (שגלול לסוף, כי שם ה-checkbox נפתח) — כלומר **התמונות המשפטיות שנשמרו עד כה כנראה הכילו רק את הסעיף האחרון של התקנון**, לא את כולו. ה-node החדש מציג את התקנון המלא בלי הגבלת גובה.
- **אומת חי בדפדפן** (Playwright, לא רק `tsc`): מילוי מלא של 3 השלבים כולל חזרה+תיקון שדה, שליחה אמיתית עד סוף התהליך, ובדיקת תוכן התמונה הנשמרת בפועל (כל 5 סעיפי התקנון מופיעים במלואם, כל הפרטים משלבים 1-2 קיימים, החתימה מוטבעת). אפס שגיאות קונסולה.
- **באג שדיווחת אחרי הבדיקה, תוקן:** ה-`useEffect` שמגדיר את גודל ה-canvas (שהוסף כדי לפתור את בעיית "גודל 0 בחזרה לשלב 3") עשה `canvas.width = rect.width` בכל הפעלה — וזה **תמיד מנקה את הביטמאפ**, גם כשהגודל לא השתנה. אז כל "חזרה" ואז "הבא" חזרה לשלב 3 מחקה את החתימה (בלי לגעת ב-`hasSigned`, ולכן גם הכיתוב "חתמו כאן" לא חזר — נראה כריבוע לבן ריק). תוקן: משנים גודל/מנקים רק אם הגודל באמת השתנה, ואם כן — מציירים בחזרה מ-`signatureDataUrl` השמור.
- **בקשה נוספת שביקשת, בוצעה:** בסעיף "פיקדון", "פיקדון ת.ז." ו"פיקדון רישיון נהיגה" אוחדו לשורה חזותית אחת ("מסמך מזהה כפיקדון") עם שתי בחירות צמודות — אבל **רק אם שני הדגלים מופעלים** בהגדרות התחנה; אם רק אחד מופעל, מוצג כשורה בודדת רגילה כמו קודם. `deposit_type` הנשלח נשאר `'id'`/`'license'` נפרד (המנהל עדיין צריך לדעת איזה מסמך פיזי הופקד). **תוקן גם חוסר-עקביות שהתגלה:** ברירת המחדל הייתה "מוצג אלא אם המנהל כיבה במפורש" — הפוך ממה שביקשת. שונה ל"מוצג רק אם המנהל הפעיל במפורש", גם בטופס הציבורי (`sign/[stationId]/page.tsx`) וגם בברירות המחדל של מודל ההגדרות (`[stationId]/page.tsx`) — משפיע על **כל** תחנה שמעולם לא נגעה בהגדרה הזו.
- **בקשה שלישית, בוצעה:** אימות ספרת-ביקורת רשמית לתעודת זהות ישראלית (`isValidIsraeliId`) — חוסם התקדמות משלב 1 אם המספר לא תקין (כולל ספרות רנדומליות כמו 123456789), ושדה הקלט מסונן לספרות בלבד תוך כדי הקלדה.
- **תיקון UI נוסף שדיווחת:** האייקונים במסך "שולח את הטופס..." ובמסך ההצלחה (ה-V הירוק) לא היו ממורכזים — כי הם `<svg>` גולמי בתוך div עם `textAlign:center` בלבד, וב-SVG זה לא מספיק (התבנית הקיימת בקובץ תמיד עוטפת SVG ב-`inline-flex` בדיוק מהסיבה הזו). תוקן ע"י `display:flex; justifyContent:center` על שני ה-wrapper-ים. אומת (0px סטייה ממרכז) ב-Playwright.
- **ממתין לפירוט ממך:** תיקון בנוסח התקנון עצמו — ראה סעיף "מחכה להחלטה/קלט שלך".

**עקרונות עבודה שנקבעו (לא קוד, נרשמו ב-`.wolf/cerebrum.md`)**
- להשתמש ב-`/punctures` כדוגמת-ייחוס בכל שיפוץ עתידי של דף אחר.

**כניסה ביומטרית (WebAuthn) — חובר ה-frontend**
- הבקאנד היה קיים במלואו מניסיון קודם (טבלאות, `lib/webauthn.ts`, 7 API routes) אבל אף קריאה אחת מה-frontend לא הייתה קיימת. חוברה עכשיו — הכל בעמוד הלוגין בלבד (`login/page.tsx`), לא בעמודי הגדרות נפרדים לכל תפקיד.
- **תוקן שורש הבעיה שגרמה לניסיון הקודם "לא לעבוד טוב":** `register/begin/route.ts` דרש `residentKey: 'required'` שגרם לדפדפן להציג picker כללי של Passkey (iCloud Keychain/Google) במקום בקשת טביעת-אצבע ישירה. שונה ל-`'discouraged'` (החלטת עבר מאושרת).
- כפתור "הפעל כניסה עם טביעת אצבע" ליד "שכחתי סיסמא" (מופיע רק אם הדפדפן תומך ב-WebAuthn ואין עדיין passkey רשום). אחרי הרשמה — כפתור "כניסה עם טביעת אצבע / פנים" מופיע אוטומטית בפעם הבאה (בדיקת `status?phone=` עם debounce תוך כדי הקלדת הטלפון).
- ביטול בקשת טביעת-אצבע ע"י המשתמש (Escape וכו') מזוהה ומטופל בשקט, בלי הודעת שגיאה אדומה מיותרת.
- **אומת חי בדפדפן** עם virtual authenticator אמיתי של Chrome DevTools Protocol (מדמה קורא טביעת-אצבע, עובר דרך אימות קריפטוגרפי אמיתי, לא מדומה): הרשמה מלאה → toast הצלחה → כפתור כניסה ביומטרית מופיע → כניסה עם הביומטרי בלבד (בלי סיסמה) → ניווט נכון לתפקיד. אומת גם שהכפתורים הנכונים מוצגים/מוסתרים לפי מצב ה-passkey. אפס שגיאות. **מגבלה שנשארה לך:** לא מחליף בדיקה על מכשיר אמיתי (iPhone/אנדרואיד) — מומלץ לבדוק בעצמך לפני שסומכים במלואו.
- **✅ ממצא אבטחה — תוקן (2026-08-21, לבקשתך):** ב-`register/begin/route.ts`, אימות הסיסמה היה **אופציונלי בפועל** (`if (password && user.password)` — אם סיסמה לא נשלחת, או שלמשתמש אין סיסמה שמורה, הבדיקה פשוט דילגה ואפשרה הרשמת passkey בלי שום הוכחת זהות, רק מספר טלפון — מי שידע רק את הטלפון של מישהו יכל לרשום את הטביעה *שלו* על החשבון שלו, בעקיפת המסך). תוקן: הסיסמה חובה עכשיו תמיד (חוסם אם אין סיסמה בבקשה, וחוסם אם למשתמש אין סיסמה שמורה בכלל). **לא משפיע על הכניסה היומיומית** — זה נוגע רק לרגע החד-פעמי של הפעלת הביומטריה, לא לכניסה עצמה (שנשארת טביעת-אצבע בלבד, בלי סיסמה). אומת ב-curl: בקשה בלי סיסמה נחסמת, סיסמה שגויה נחסמת, סיסמה נכונה עדיין עובדת.
- **מגבלה תשתיתית ל-production:** `getRpConfig()` נופל ל-`localhost` אם `NEXT_PUBLIC_RP_ID`/`NEXT_PUBLIC_WEBAUTHN_ORIGIN` לא מוגדרים — עובד מצוין בפיתוח, אבל **בפריסה אמיתית חובה להגדיר את שניהם לדומיין האמיתי** לפני שמשתמשים אמיתיים ינסו את זה.

---

## 🔧 פתוח — ממתין לביצוע

**גדול**
- לפרק את `[stationId]/page.tsx` (7,737 שורות) לרכיבי-משנה לפי טאב + hook משותף לניהול Escape/מודלים
- Widget נגישות — **הוחלט (2026-08-19): בנייה עצמית**, לא שירות מוכן כמו UserWay/נגיש לי. טרם תוכנן היקף (אילו פקדים/כמה תקן 5568 לכסות) — צריך שיחת-תכנון נפרדת לפני מימוש

**בינוני**
- Pagination אמיתי / חיפוש בצד שרת ל-`admin/vehicles` (טוען היום את כל הטבלה בבת אחת)
- לאחד את רשימת דגמי הרכב (`admin/vehicles`) ורשימת המלאי (`super-manager`) לתצוגה רספונסיבית אחת — בדגש על נוחות במובייל
- כפתור "בדוק סטטוס בקשה" למוקדן (בדיקה חד-פעמית מול השרת, לא real-time)

**פתוח, לא לגעת כרגע (הוחלט במפורש לדחות)**
- שיתוף לוגיקת התאמת-רכב משותפת בין כל דפי החיפוש הפנימיים — לא הובן/אושר עדיין

---

## ⏳ מחכה להחלטה/קלט שלך

- **🔍 באג אמיתי ב-`admin/vehicles` — סינון לפי שנה מתעלם מ-`year_to`** (`page.tsx:1274-1279`). רכב שתקף ל-2010–2020 לא יופיע בחיפוש לשנת 2015, כי הקוד בודק רק `year_from` נגד שני הגבולות (במקום גם `year_to`). אומת שוב ונדחה במפורש ב-2026-08-19 — **עדיין שמור בצד, לא לתקן כרגע**. כולל חשד נוסף לא-מאומת ל"התאמה מטושטשת" עברית אגרסיבית מדי (`normalizeHebrew` ב-`page.tsx:1161-1176` מוחקת כל אות א׳ בכל מקום במחרוזת, לא רק בתפקיד תנועה — יכול לגרום להתאמות-שווא בין דגמים שונים).
- **תיקון בנוסח התקנון** (`termsItems` ב-`sign/[stationId]/page.tsx`) — ביקשת לתקן משהו בנוסח, עוד לא פירטת מה. התקנון קיים רק בקוד (אין שדה DB/מסך ניהול לעריכתו — ראה גם את הפריט למטה). לחכות לפירוט הנוסח המדויק.

---

## ❌ נדחה / לא רלוונטי (לא לפעול)

- SMS לשחזור סיסמה — לא נדרש; שמירת תמונה של תעודת השחזור מספיקה, והגיבוי המכוון הוא פנייה ידנית לאדמין (אין SMS בפרויקט, עלות)
- להעלות "חסום מוקדן" מחוץ לתפריט "⋮" — נשאר כמו שהוא
- חיווי "פנצ'ריות דורש כניסה נפרדת" — אין באמת חוויה כפולה; כולם נכנסים דרך `/login` הראשי
- להסיר state `activeTab` ב-`super-manager` — **בטל**, זה לא state כפול/מת (שולט על מסך drill-down נפרד מ-`mainTab`)
- חלופת הקלדת שם לחתימה בטופס הציבורי — בוטל, נשארת רק חתימה בציור
- תקציר מקוצר לתקנון בטופס — נדחה, התקנון לא ארוך מדי כמו שחשבנו
- תיוג "בעיה ידועה" ברמת דגם-גלגל — לא רלוונטי, אין קונספט "דגם גלגל" במערכת (הגלגלים הם אוסף מצטבר של פריטים בודדים)

---

## תיקונים לדוח המקורי (לזכרון — לא פעולות)

1. שחזור סיסמה למנהל תחנה לא דורש הדפסה — שמירת תמונה מספיקה.
2. `super-manager/page.tsx`'s `activeTab` אינו state מת — הוא שולט על מסך אחר (drill-down לתחנה בודדת).
3. `/stations` **אינו** ציבורי — דורש התחברות בדיוק כמו `/search` (הדוח המקורי טעה בזה).
4. `/admin/punctures/login` לא באמת בשימוש — כולם נכנסים דרך `/login` הראשי.
