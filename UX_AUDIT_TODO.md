# סיכום החלטות — ביקורת UX (2026-08-19)

מסמך עבודה שנוצר אחרי מעבר משותף על כל ההמלצות מדוח ביקורת ה-UX (כל 8 סוגי המשתמשים + פיצ'רים מוצעים). כולל גם כמה תיקונים לדוח המקורי שהתגלו תוך כדי השיחה. שום דבר מכאן **לא יושם בפועל** — זו רשימת עבודה לעתיד.

> דוח מקורי (Artifact): https://claude.ai/code/artifact/f964306a-b050-4254-a138-4ac9e2798f60 — יש בו כמה טעויות שתוקנו כאן (ראה "תיקונים לדוח" בסוף).

---

## 1. מנהל תחנה (`src/app/[stationId]/page.tsx`)

- [ ] לפרק את הקובץ (7,737 שורות) לרכיבי-משנה לפי טאב (WheelsTab / TrackingTab / AlertsTab / ReportsTab) + hook משותף אחד לניהול Escape/מודלים
- [ ] לאחד את טאב "מעקב" לתצוגה רספונסיבית אחת (CSS Grid, כמו טאב "מלאי") במקום טבלה+כרטיסים כפולים
- [x] **בוצע** — מנגנון ההתחברות הישן (`wheel_manager_{stationId}`) נמחק לגמרי מכל הקוד (7 מקומות ב-`[stationId]/page.tsx` + `AppHeader.tsx` + `search/page.tsx` + `stations/page.tsx` + Storybook mocks). חלון ההתחברות עצמו נשאר לתהליך שחזור סיסמה, אך כותב עכשיו לפורמט החדש (`station_session_`). כתוצאה מזה נמצא ונמחק גם `GET`/`verifyToken`/`SESSION_EXPIRY_MS` היתומים ב-`api/wheel-stations/[stationId]/auth/route.ts` (אומת שאין להם קוראים בכלל, כולל לא בשחזור סיסמה — זה נתיב נפרד `/api/wheel-stations/recovery`). `tsc --noEmit` עובר נקי.
- [x] ~~SMS לשחזור סיסמה~~ — **נדחה**. התעודה לא דורשת הדפסה (שמירת תמונה מספיקה), והגיבוי המכוון הוא פנייה ידנית לאדמין. אין SMS בפרויקט (עלות).

## 2. מוקדן (`src/app/operator/page.tsx`)

- [ ] Hook/קוד משותף אחד לניהול "החלפת תפקיד" — במקום 4 עותקים נפרדים (AppHeader, מוקדן, מנהל-מוקד, AdminSidebar)
- [ ] **חיפוש רכב — התנהגות זהה במוקדן וב-`/search`:** כניסה לחיפוש = מיד שדה לוחית גדול עם פוקוס אוטומטי, ומתחתיו קטן יותר 2 האפשרויות הנוספות (דגם/מפרט). בלי קליק נוסף על טאב. (חל גם על סעיף 7 למטה)
- [x] **בוצע** — נוצר `DEV_NOTES.md` בשורש הפרויקט עם התזכורת (כבר נשכח פעם אחת בעבר, bug-129)

## 3. מנהל מוקד (`src/app/call-center/page.tsx`)

- [x] **בוצע** — כותרת ברורה בראש שני הדפים: "ניהול המוקד שלי" (+שם המוקד) ב-`/call-center`, "ניהול כל המוקדים במערכת" ב-`/admin/call-centers`
- [x] ~~להעלות "חסום מוקדן" מחוץ לתפריט "⋮"~~ — **נדחה**, נשאר כמו שהוא

## 4. אדמין (`src/app/admin/**`)

- [ ] Pagination אמיתי / חיפוש בצד שרת ל-`admin/vehicles` (טוען כרגע את כל הטבלה בבת אחת)
- [ ] לאחד את שתי הרשימות (טבלת דגמי רכב ב-`admin/vehicles`, מלאי ב-`super-manager`) לתצוגה רספונסיבית אחת (CSS Grid) — **בדגש על נוחות/נגישות במובייל**, כי שם רוב השימוש בפועל
- [x] ~~חיווי "פנצ'ריות דורש כניסה נפרדת"~~ — **נדחה**. `admin/punctures/login` לא מקושר משום מקום, כולם נכנסים תמיד דרך `/login` הראשי. אין חוויה כפולה בפועל.
- [x] **בוצע** — מתג "הצג מוצפן" בדף `admin/users` נמחק לגמרי (state + כפתור + לוגיקת סינון). `tsc --noEmit` עובר נקי.

### 🔍 באג אמיתי שנמצא (לא היה בדוח המקורי) — טרם הוחלט מה עושים
**סינון לפי שנה ב-`admin/vehicles` מתעלם מ-`year_to` של הרכב** (`page.tsx:1274-1279`) — רכב שתקף לשנים 2010–2020 **לא יופיע** בחיפוש לשנת 2015, כי הקוד בודק רק את `year_from`. אומת עם דאטה אמיתי מה-DB (יש שורות עם טווח שנים רחב, למשל VW Beetle 1946–1969). **המשתמש ביקש לשמור בצד ולטפל בהמשך** — כולל גם חשד נוסף (לא מאומת עדיין) ל"התאמה מטושטשת" עברית אגרסיבית מדי (`normalizeHebrew` מוחקת כל אות א׳) שעלולה לגרום להתאמות שגויות.

## 5. מנהל מחוז (`src/app/super-manager/page.tsx`)

- [x] ~~להסיר state מת `activeTab`~~ — **בטל, אין מה לנקות.** התברר שזה לא state כפול/מת — `activeTab` שולט על הטאבים במסך הפרטני של תחנה בודדת (drill-down), בעוד `mainTab` שולט על המסך הראשי ברמת המחוז. שני מסכים שונים, לא כפילות.
- [x] **בוצע** — קובץ הנתיב `src/app/api/super-manager/auth/route.ts` נמחק (אומת: אפס קוראים בכל הפרויקט). `verifySuperManager`/`super-manager-auth.ts` נשארו בדיוק כמו שהיו.
- [ ] (כבר נכלל בסעיף 4) איחוד רשימת המלאי לתצוגה רספונסיבית אחת

## 6. שואל גלגל — ציבור (`src/app/sign/[stationId]/page.tsx`)

- [ ] לפצל את הטופס ל-3 מסכים (wizard: זהות ← רכב/ערבון ← תקנון+חתימה), עם כפתור "המשך" בין שלב לשלב
- [x] ~~חלופת הקלדת שם לחתימה~~ — **בוטל**, נשארת רק חתימה בציור, בלי חלופת טקסט בכלל
- [x] ~~תקציר מקוצר לתקנון~~ — **נדחה**, התקנון לא ארוך מדי כמו שחשבנו

## 7. חיפוש ציבורי (`/lookup`, `/reverse-search`, `/stations`, `/search`)

- [x] **בוצע** — `/lookup` עכשיו מפנה (client redirect) ל-`/reverse-search` עם הודעת "מעביר אותך..." קצרה. הקוד הישן שלו הוסר לגמרי.
- [ ] (חלק 2, **לא לגעת כרגע**) שיתוף לוגיקת התאמה משותפת בין כל הדפים הפנימיים — נשאר פתוח, לא הובן/אושר עדיין
- [x] ~~קישור שבור `/lookup` → `/search`~~ — **נפתר אוטומטית**
- [x] **בוצע** — הודעת ביניים ("צריך להתחבר... מעביר אותך למסך התחברות") במקום מסך לבן/לודינג אינסופי, גם ב-`/search` וגם ב-`/stations`

## 8. מדריך פנצ'ריות (`src/app/punctures/page.tsx`)

- [x] **הוחלט כעיקרון עבודה קבוע** (נרשם ב-`.wolf/cerebrum.md`): להשתמש ב-`/punctures` כדוגמת-ייחוס בכל שיפוץ עתידי של דף אחר — לא ספריית רכיבים פורמלית נפרדת, אלא "להעתיק דפוסים משם" כברירת מחדל

## פיצ'רים מוצעים (נוספים על מה שכבר בסעיפים למעלה)

- [ ] **לקדם WebAuthn (כניסה ביומטרית)** — כבר יש תיעוד מלא מניסיון קודם (`.claude/projects/.../memory/webauthn_biometric_login_paused.md`): הבעיה הקודמת הייתה `residentKey: 'required'` שכפה חוויית Passkey מסורבלת; ההחלטה כבר קיימת — לעבור ל-`residentKey: 'discouraged'` (קשור-למכשיר, לא Passkey מסונכרן), כי כל משתמש כאן משתמש במכשיר קבוע אחד. 4 צעדים מוכנים במסמך.
- [x] ~~תיוג "בעיה ידועה" ברמת דגם-גלגל~~ — **נדחה**, לא רלוונטי. אין קונספט "דגם גלגל" במערכת — הגלגלים הם אוסף מצטבר של פריטים בודדים, לא קטלוג לפי דגם יצרן.
- [ ] **סטטוס בקשה למוקדן — גרסה מפושטת:** לא real-time/push, אלא כפתור פשוט "בדוק סטטוס בקשה" על תוצאת החיפוש (בדיקה חד-פעמית מול השרת בלחיצה)

- [ ] **רעיון חדש למנהל תחנה — "שכחתי סיסמה" דרך WhatsApp:** מודאל עם שם מלא + טלפון (חובה) + סיסמה מבוקשת (אופציונלי) → פותח WhatsApp עם כל הנתונים, ישירות אליך. תיעוד מלא נשמר ב-`project_forgot_password_whatsapp_idea.md` בזיכרון החוצה-סשנים.

## 🆕 נמצא תוך כדי השיחה, לא היה בדוח המקורי

- [ ] **אין widget נגישות בכלל** (הגדלת טקסט / ניגודיות / עצירת אנימציות וכו') — יש רק עמוד הצהרת נגישות (`/accessibility`) בלי שום מימוש בפועל באתר. אושר להוסיף כהמלצה; אופן היישום (שירות מוכן כמו UserWay/"נגיש לי" מול בנייה עצמית) — טרם הוחלט.

---

## תיקונים לדוח המקורי (לזכרון — לא פעולות)

1. שחזור סיסמה למנהל תחנה לא דורש הדפסה — שמירת תמונה מספיקה.
2. `super-manager/page.tsx`'s `activeTab` אינו state מת — הוא שולט על מסך אחר (drill-down לתחנה בודדת).
3. `/stations` **אינו** ציבורי — דורש התחברות בדיוק כמו `/search` (הדוח המקורי טעה בזה).
4. `/admin/punctures/login` לא באמת בשימוש — כולם נכנסים דרך `/login` הראשי.
